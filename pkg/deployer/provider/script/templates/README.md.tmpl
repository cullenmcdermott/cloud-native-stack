# Cloud Native Stack Bundle

Generated: {{ .Timestamp }}
{{- if .RecipeVersion }}
Recipe Version: {{ .RecipeVersion }}
{{- end }}

## Components

This bundle contains the following components:

{{- range .Components }}
- **{{ .Name }}** ({{ .Version }})
{{- end }}

## Manual Deployment

Each component directory contains:

- `values.yaml` - Helm chart configuration
- `manifests/` - Kubernetes manifests (if applicable)
- `scripts/install.sh` - Installation script
- `scripts/uninstall.sh` - Uninstallation script
- `checksums.txt` - SHA256 checksums for verification

### Installation Steps

1. Verify checksums:
   ```bash
   cd <component-dir>
   sha256sum -c checksums.txt
   ```

2. Deploy components in order:
{{- range .Components }}
   ```bash
   cd {{ .Name }}/
   chmod +x scripts/install.sh
   ./scripts/install.sh
   ```
{{- end }}

3. Verify deployment:
   ```bash
   kubectl get pods -A
   ```

## Notes

- Ensure you have cluster-admin permissions
- Review `values.yaml` in each component directory before deployment
- Installation scripts may require environment-specific modifications
